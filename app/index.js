var React = require('react');
var ReactDOM = require('react-dom');
// var Jammers = require('./components/Jammers');
var Jammers = require('./components/Jammers.new');
var App = require('./components/App');

ReactDOM.render (
    <Jammers />,
    document.getElementById('app')
);